[project]
name = "open-banca-banco-general"
version = "0.1.0"
description = "Banco General bank driver: map.json, parser.json, fixtures, tests"
requires-python = ">=3.12"
license = { text = "AGPL-3.0" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/open_banca_banco_general"]
