{
  "name": "storage",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/adapters/storage/src",
  "tags": [
    "scope:storage",
    "layer:adapter"
  ],
  "implicitDependencies": [
    "application",
    "domain"
  ],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run ruff check packages/adapters/storage",
        "cwd": "{workspaceRoot}"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run pyrefly check packages/adapters/storage/src",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run pytest packages/adapters/storage/tests -v",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
