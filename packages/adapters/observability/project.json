{
  "name": "observability",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/adapters/observability/src",
  "tags": [
    "scope:observability",
    "layer:adapter"
  ],
  "implicitDependencies": [
    "application",
    "domain"
  ],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run ruff check packages/adapters/observability",
        "cwd": "{workspaceRoot}"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run pyrefly check packages/adapters/observability/src",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run pytest packages/adapters/observability/tests -v",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
