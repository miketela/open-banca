# PRD: open-banca v1

**Author:** Mike Portela
**Date:** 2026-05-09
**Status:** Approved
**Version:** 1.0
**Taskmaster Optimized:** Yes

---

## Executive Summary

Panama no tiene Open Banking. Acceder a información financiera propia requiere entrar a la web del banco y descargar reportes Excel manualmente — tedioso, no automatizable de forma confiable porque las webs cambian.

`open-banca` es una API REST self-hosted, open source AGPL-3.0, que automatiza la captura de datos bancarios usando agentes de IA + Playwright. Un agente Mapper genera un `map.json` declarativo, un runner Playwright sin LLM lo ejecuta cada corrida, agentes Validator/Judge supervisan, y un Remapper repara cuando el flujo cambia. Banco piloto: **Banco General**. Goal secundario: presión institucional sobre bancos PA — la licencia AGPL fuerza que cualquier servicio derivado quede abierto.

Impacto esperado: usuario puede consumir su data financiera vía un GET autenticado en lugar de un proceso manual repetitivo, con resilience automática frente a cambios de UI del banco.

---

## Problem Statement

### Current Situation

Bancos de Panamá (Banco General, BAC, Banistmo, Global Bank, etc.) no exponen Open Banking ni APIs públicas. La única vía de acceso a la propia información financiera es la banca en línea. Cada usuario debe:

1. Loguearse (típicamente con OTP — Banco General usa Clave Móvil push).
2. Navegar a cada cuenta (ahorro, corriente, tarjeta de crédito, préstamo).
3. Configurar rango de fechas.
4. Descargar Excel.
5. Repetir por cada cuenta.
6. Procesar manualmente para análisis o consolidación con otras cuentas.

Las webs cambian sin aviso. Cualquier scraper "tradicional" (selectores hardcoded) rompe con cada redeploy del banco.

### User Impact

- **Quién**: usuarios self-hosted del proyecto — desarrolladores, contadores, herramientas de finanzas personales como `maybe`, fintechs PA que quieran agregar data sin convenios bancarios.
- **Pain**: 15–30 min/mes por cuenta para consolidar; alto riesgo de errores por copy-paste manual; imposibilidad de automatizar dashboards o alertas.
- **Severidad**: Alta para usuarios con 4+ cuentas; bloqueante para herramientas de PFM en mercado PA.

### Business Impact

- **Costo del problema**: barrera estructural a fintechs PA. Imposibilita productos financieros de tercera generación (PFM, agregadores, advisors) sin convenios uno-a-uno con cada banco.
- **Oportunidad**: open-source la solución → presión institucional, demuestra que la data ya es accesible al usuario, mal entregada por defecto.
- **Strategic**: alineado con tendencia regional (México, Brasil, Chile, UE) hacia Open Banking; se anticipa al regulador.

### Why Solve This Now?

- LLMs con vision (Claude Sonnet 4.6, GPT-5) ya son capaces de mapear flujos web complejos confiable y barato.
- DeepSeek V3 hace operación recurrente (validator/judge) viable a costo near-zero.
- Librería `browser-use` resolvió el ecosistema CDP/agent loop con calidad production.
- Temporal + sqlcipher hacen self-hosting realista sin reinventar durable execution.

---

## Goals & Success Metrics

### Goal 1: Captura confiable de Banco General (piloto)

- **Métrica**: success rate de scrape jobs (terminan en `job.completed`).
- **Baseline**: 0% (no existe).
- **Target**: ≥ 95% sobre 100 corridas reales en 30 días.
- **Timeframe**: 60 días post-lanzamiento de v1.
- **Measurement**: contador en SQLite (P0, instrumentación mínima obligatoria). Langfuse dashboard P1 opcional (Goal P0 no puede depender de componente P1).

### Goal 2: Self-healing efectivo (HITL-supported en v1)

- **Métrica (v1, post ADR-0013-amendment)**: % de breakages detectados que se resuelven con UN solo HITL approval (no escalan a multi-touch / multiple proposals rechazados).
- **Baseline**: 0% (no existe).
- **Target v1**: ≥ 70% de breakages → 1-shot HITL approval → re-corrida exitosa. Auto-remap measurement **deferred a v1.x** post-baseline empírico (mín. 100 decisiones humanas para calibrar Judge confidence/risk).
- **Timeframe**: 90 días.
- **Measurement**: log de Judge (telemetría) + outcome de HITL approval + success de re-corrida en SQLite + Langfuse (opcional).

### Goal 3: Costo por scrape acotado

- **Métrica**: USD promedio de costo LLM por scrape job exitoso.
- **Baseline**: n/a.
- **Target**: ≤ $0.10/scrape en estado estable (incremental, sin remap). Mapping inicial puede llegar a $0.50.
- **Timeframe**: 60 días.
- **Measurement**: cost guardrails enforcement + LiteLLM cost tracking.

### Goal 4: Self-host viable

- **Métrica**: tiempo desde `git clone` hasta primer scrape exitoso para un dev externo siguiendo docs.
- **Target**: ≤ 30 minutos.
- **Timeframe**: validación pre-lanzamiento.
- **Measurement**: prueba con 3 betatesters externos.

### Goal 5: Seguridad de credenciales

- **Métrica**: 0 fugas de credenciales bancarias del usuario en logs/traces/HAR (verificado con canary test).
- **Target**: 100% (cero tolerancia).
- **Measurement**: canary CI test en cada PR.

---

## User Stories

### Story 1: Primer scrape de un banco recién agregado

**Como** developer self-hosting open-banca,
**Quiero** dar credenciales de mi cuenta Banco General y obtener un dump completo de mis transacciones históricas (últimos 6 meses, límite del banco),
**Para** consolidar finanzas en mi sistema sin descarga manual.

**Acceptance Criteria:**
- [ ] `POST /scrape {bank: "banco_general", credential_ref: "...", full: true}` devuelve `job_id` en < 1s.
- [ ] Webhook `job.created` se dispara inmediatamente.
- [ ] Si no existe `map.json` para Banco General, Mapper agent corre automáticamente. Observables: webhook `job.mapping_started` emitido al disparar Mapper, hard cap 5 min de duración, si falla emite `job.failed reason: mapping_failed` con detalle del último step intentado.
- [ ] Webhook `job.otp_required` dispara cuando llega a Clave Móvil.
- [ ] Estado `awaiting_otp` tiene TTL hard de 4 min. Al timeout: workflow emite `job.failed reason: otp_timeout` y cierra sesión browser. State machine: `awaiting_otp` → (`otp_confirmed` signal) → `otp_confirmed` | (timer expira) → `otp_timeout` → `failed`.
- [ ] Cliente confirma OTP via `POST /jobs/{id}/otp-confirmed` dentro de 4 min.
- [ ] Excel descargados por cada cuenta detectada (ahorro, corriente, TC).
- [ ] Parser DSL convierte Excel → schema canónico unificado.
- [ ] Webhook `job.completed` dispara con link a `GET /jobs/{id}/result`.
- [ ] Result incluye: cuentas, balances, transacciones (deduplicadas).

**Task Breakdown Hint:**
- API + Temporal scaffolding (16h).
- Mapper agent integration browser-use (12h).
- Scraper runner DSL executor (16h).
- Excel parser DSL engine (12h).
- Banco General initial mapping run (8h).
- Webhook emitter + HMAC (6h).
- E2E con HAR fixture (10h).

**Dependencies**: REQ-001..REQ-008.

---

### Story 2: Scrape incremental periódico

**Como** usuario con scrape inicial completo,
**Quiero** correr `POST /scrape {since: <last_cursor>}` periódicamente para traer sólo nuevos movimientos,
**Para** mantener mi dashboard actualizado sin re-procesar todo.

**Acceptance Criteria:**
- [ ] `since` cursor se persiste por cuenta tras cada scrape exitoso.
- [ ] Range de descarga = ventana desde `since - 3 días` (buffer) hasta hoy.
- [ ] Dedup en 3 niveles: ID embebido del banco → fingerprint hash → fuzzy transfers cross-account.
- [ ] Transferencias internas detectadas y emparejadas (no cuentan dos veces).
- [ ] Result devuelve sólo registros nuevos.

**Task Breakdown Hint:**
- Cursor storage + API params (4h).
- Dedup engine 3-level (12h).
- Transfer matching cross-account (8h).
- Tests con fixture (6h).

**Dependencies**: Story 1.

---

### Story 3: Self-healing cuando el banco cambia

**Como** operador self-hosted,
**Quiero** que cuando Banco General cambie un selector o flujo, el sistema detecte la ruptura, intente un remap automático, y solo me alerte si requiere mi review,
**Para** no maintenance manual constante.

**Acceptance Criteria:**
- [ ] Scraper runner emite `BreakageEvent` cuando: selector null, timeout, schema mismatch, HTTP error en step conocido.
- [ ] Judge agent (DeepSeek) recibe el evento + screenshot + último step y emite decisión + `confidence` + `risk`.
- [ ] **v1 (ADR-0013-amendment)**: SIEMPRE se emite webhook `job.remap_proposed` con diff del map + `confidence` + `risk` adjuntos como telemetría. NO auto-apply en v1.
- [ ] Auto-apply path (`confidence ≥ 0.85 AND risk == low → aplicar sin HITL`) está implementado pero feature-flagged OFF en v1, on por config opt-in del operador. Default OFF.
- [ ] Endpoint `POST /maps/{bank}/proposals/{id}/approve` aplica el patch.
- [ ] TTL del proposal: 24h.
- [ ] Cap: max 3 remap attempts por banco por 24h.

**Task Breakdown Hint:**
- BreakageEvent contract + emission (6h).
- Judge agent PydanticAI + DeepSeek (10h).
- Remapper agent browser-use + Claude (12h).
- Auto-apply + git commit map (4h).
- HITL endpoint + proposal state (8h).

**Dependencies**: Story 1.

---

### Story 4: Cuentas múltiples por login

**Como** usuario Banco General con savings + checking + 2 tarjetas crédito,
**Quiero** que un solo scrape job traiga todas las cuentas detectadas en mi login,
**Para** no hacer 4 jobs separados.

**Acceptance Criteria:**
- [ ] Default = detect-all (sin pasar `accounts:[]` filter).
- [ ] Schema unificado con discriminator `account_type` (savings, checking, credit_card).
- [ ] TC payload incluye: credit_limit, available_credit, cut_date, min_payment, payment_due_date, statement_balance.
- [ ] Cada cuenta tiene su propia descarga Excel (formato puede diferir entre savings y TC).
- [ ] Préstamos NO en v1 (out of scope).

**Dependencies**: Story 1.

---

### Story 5: Operador hace setup en 30 min

**Como** developer que clona el repo por primera vez,
**Quiero** correr `docker-compose up` y tener API levantada con un comando claro para registrar primer banco,
**Para** validar la herramienta sin complejidad de infra.

**Acceptance Criteria:**
- [ ] `docker-compose.yml` levanta: api, temporal-server, temporal-worker, sandbox-runner, postgres (para Temporal). Langfuse opcional via profile.
- [ ] `OPEN_BANCA_MASTER_PASSPHRASE` env o prompt al boot.
- [ ] CLI `open-banca register-credentials --bank banco_general` interactivo.
- [ ] Setup docs: README + docs/05-operations/deployment.md con paso a paso.
- [ ] First scrape funciona sin tocar código.

**Dependencies**: Stories 1, 4.

---

### Story 6: Operator handles wrong credentials / account lockout

**Como** operador self-hosted,
**Quiero** que cuando el login falle, el sistema clasifique el tipo de fallo y aplique la política correcta (circuit breaker inmediato para credenciales malas, retry para push rechazado, backoff para errores de red),
**Para** no bloquear mi cuenta bancaria por reintentos automáticos sobre credenciales inválidas.

**Acceptance Criteria:**
- [ ] Cuando LoginActivity falla, clasificador determina `error_type`: `credential_error` | `push_rejected` | `network_error` | `unknown`.
- [ ] `credential_error` (username/password rechazado por el banco): circuit breaker INMEDIATO 1h cooldown tras 1 fallo (NO esperar 2 fallos como caso genérico). Emite `job.failed reason: credential_error`. Bloquea futuros jobs sobre esa `credential_ref` durante 1h.
- [ ] `push_rejected` (usuario rechaza Clave Móvil push activamente): retry once con nueva push. Si segunda push también rechazada → `job.failed reason: push_rejected_twice`. No circuit breaker.
- [ ] `network_error` (timeout TCP, DNS, 5xx del banco): retry con exponential backoff (3 intentos, base 2s). No cuenta hacia circuit breaker de credenciales.
- [ ] `unknown`: comportamiento conservador — circuit breaker 1h (tratado como `credential_error`).
- [ ] Webhook `job.failed` payload incluye `error_type` y `error_subtype` para que el operador pueda automatizar respuesta.
- [ ] Clasificador es determinístico (basado en HTTP status, texto de error, presencia de selectores específicos) y testeable con HAR fixtures.
- [ ] Override manual: operador puede resetear circuit breaker via CLI `open-banca circuit-breaker reset --bank banco_general --credential-ref <ref>` con confirmación interactiva.

**Task Breakdown Hint:**
- Login error classifier + tests con HAR fixtures (8h).
- Circuit breaker con clasificación por tipo (6h).
- CLI override + audit log (4h).

**Dependencies**: Story 1, REQ-011 (cost guardrails / circuit breaker base).

---

## Functional Requirements

### Must Have (P0)

#### REQ-001: API REST FastAPI

10 endpoints (ver `docs/02-components/api.md`):
- `POST /scrape`, `GET /jobs/{id}`, `GET /jobs/{id}/result`, `POST /jobs/{id}/otp-confirmed`, `POST /jobs/{id}/cancel`.
- `POST /maps/{bank}/proposals/{id}/approve|reject`.
- `GET /banks`, `GET /accounts`, `POST /webhooks/test`.

**AC**: idempotency keys, rate limiting por banco, bearer token auth single-org, OpenAPI 3.1 spec auto-generada.

#### REQ-002: Temporal orquestador

`ScrapeJobWorkflow` + child workflows (`MapBankWorkflow`, `RemapBankWorkflow`) + 11 activities (`LoginActivity`, `NavigateActivity`, `DownloadExcelActivity`, `ParseExcelActivity`, `ValidateActivity`, `JudgeActivity`, `MapperAgentActivity`, `RemapperAgentActivity`, `EmitWebhookActivity`, `OTPSignalAwaitActivity`, etc.) + 3 signals (`otp_confirmed`, `remap_approved`, `cancel_job`). Detalle en `docs/02-components/orchestrator.md`.

**AC**: durabilidad cross-restart, time-skipping testing, replay determinístico.

#### REQ-003: Mapper agent (browser-use + Claude vision)

Agente que explora login + dashboard + descarga Excel y emite `map.json` declarativo. Usa `sensitive_data` para que el LLM nunca vea creds plaintext.

**AC**: produce JSON Schema-valid, ejecutable por Scraper runner sin modificación.

#### REQ-004: Scraper runner Playwright sin LLM

Lee `map.json`, ejecuta steps secuencialmente, emite `BreakageEvent` al fallar. 9 step types (navigate, click, fill, wait_for_selector, wait_for_download, select_date_range, assert_text, extract_table, download_file). Detalle en `docs/02-components/scraper-runner.md`.

**AC**: 0 llamadas LLM en hot path. Replay-able contra HAR fixture.

#### REQ-005: Validator + Judge agents (PydanticAI + DeepSeek)

Validator hace checks heurísticos + LLM cuando ambiguo. Judge analiza BreakageEvent + screenshot y decide ruta (retry / partial_remap / full_remap / abort / human_required) + confidence + risk.

**AC (v1, post-review ADR-0013-amendment)**: **v1 es HITL-only**: siempre se emite webhook `job.remap_proposed` y se espera approval humano via endpoint. `confidence` y `risk` se computan y persisten **solo como telemetría** (para futura calibración). El path de auto-apply (confidence ≥ 0.85 AND risk == low) queda **deferred a v1.x** una vez que existan ≥ 100 decisiones humanas reales sobre las cuales calibrar los thresholds. Justificación: Judge text-only sobre OCR de screenshot tiene calibración admitida como "arbitraria"; activar auto-apply sin baseline empírica = blast radius alto. Ver ADR-0013-amendment.

#### REQ-006: Remapper agent (browser-use + Claude vision)

Re-explora la zona afectada y emite patch al `map.json`. Auto-apply o emit webhook según Judge.

**AC**: dry-run validation antes de full apply (sin enviar creds reales).

#### REQ-007: Excel parser declarativo (DSL)

Helpers whitelisted: `parse_date`, `extract_regex`, `normalize_amount`, `lookup_table`, `coalesce`, `trim`, `to_upper`, `to_lower`, `concat`. `parser.json` por banco define column mappings. account_type inference. id_strategy (embedded vs fingerprint). Detalle en `docs/02-components/parser.md`.

**Nota (post P0-4 review)**: `extract_regex` backed by `re2` (no ReDoS, linear time guarantees). Excel parsing wrapped with `defusedxml.lxml` + size/zip-ratio guards (max uncompressed size, max ratio compressed:uncompressed). `lookup_table` con cap explícito de memoria. Ver ADR-0007-amendment.

**AC**: NO ejecuta Python arbitrario; ejecuta dentro del sandbox sin riesgo RCE; no vulnerable a ReDoS o XML zip-bomb (CWE-1333, CVE-2017-5992). Tests con payloads maliciosos en CI.

#### REQ-008: Storage SQLite + sqlcipher

Tablas: credentials, jobs, accounts, transactions, dedup_index, audit_log, webhook_outbox. Maps en filesystem versionados con git tags. Dedup 3 niveles. Detalle en `docs/02-components/storage.md`.

**AC**: secrets cifrados con Argon2id KDF + AES-GCM. Backup del SQLite cifrado es opaco sin master passphrase.

#### REQ-009: Sandbox Docker per-job

Container efímero por scrape. Network policy: allowlist (dominio banco + APIs LLM). Drop everything else. caps dropped, seccomp profile, AppArmor, read-only rootfs (excepto tmpfs y /downloads), no-new-privileges, user non-root, resource limits (cpu, mem, pids). Detalle en `docs/04-security/sandbox.md`.

**Nota (post P0-1 review, ADR-0019)**: el sandbox aloja un proceso `BrowserSidecar` que mantiene la sesión CDP viva cross-restart del worker Temporal durante OTP wait. IPC entre worker y sidecar: Unix Domain Socket `/run/banca/sidecar.sock` (dentro del container). Token `browser_session_token = {container_id, socket_path, sidecar_pid}` persistido en Temporal activity heartbeat. Lifecycle del sidecar PID: nace con el container, muere con job.completed/failed/cancelled. TTL configurable via `BANCA_OTP_SIDECAR_TTL_S` (default 300s).

**AC**: container se destruye al terminar job. Filesystem no persiste cross-job. Sidecar PID monitoreado por health check; si muere durante OTP wait, workflow emite `job.failed reason: sidecar_died`.

#### REQ-010: Webhooks HMAC-SHA256

7 eventos: `job.created`, `job.otp_required`, `job.progress`, `job.completed`, `job.failed`, `job.remap_proposed`, `job.human_required`. Header `X-OpenBanca-Signature: t=<ts>,v1=<hmac>`. Retry exponential hasta 6 intentos en 24h. Anti-replay window ±5 min. Endpoint `/time` para diagnosis clock skew. Detalle en `docs/02-components/webhooks.md`.

**AC**: cliente puede verificar firma con HMAC-SHA256 + secret compartido. DLQ si excede retry budget.

#### REQ-011: Cost guardrails

- Max **$0.50 LLM cost/job** (mapper Claude más caro; validator/judge DeepSeek baratos).
- Max **3 remap attempts por banco por 24h**.
- Max **1 mapping run por banco por 24h** (defensivo, override por config).
- Max **2 logins fallidos consecutivos** → circuit breaker 1h.
- Max tokens per agent per job, abort si excede.

**AC**: enforcement central (no por-agente). `job.failed` con `reason: budget_exceeded`.

#### REQ-012: Banco General piloto end-to-end

`banks/banco_general/map.json` + `parser.json` + fixtures HAR + tests. Auth con Clave Móvil. Cuentas: savings, checking, credit_card. Detalle + open questions en `docs/06-banks/banco-general.md`.

**AC**: 95% success rate sobre 100 scrapes reales.

#### REQ-018: docker-socket-proxy hardening (PROMOVIDO A P0)

Golden config + smoke test endpoints prohibidos. **Issue #2.** **Justificación promoción**: Phase 3 piloto correrá con socket exposure si esto queda en P2; cualquier CVE de Chromium = host compromise. Inconsistente con REQ-009 (sandbox) que ya está P0.

**AC**: Worker no tiene acceso directo al socket Docker. Proxy expone solo endpoints whitelisted (CONTAINERS=1, NETWORKS=1, BUILD=0, EXEC=0). Smoke suite valida allow/deny por endpoint.

#### REQ-021: PII filter middleware on LLM boundary (NUEVO P0)

Filter middleware de dos capas que redacta PII del usuario antes de mandar payloads al LLM provider, para cumplir Ley 81 PA Art. 13 (transferencia transfronteriza de datos personales requiere consentimiento explícito). **Justificación**: browser-use `sensitive_data` solo redacta credenciales declaradas. Post-login el banco renderiza nombre titular, número de cuenta, balance, dirección, etc. — todo eso llega al provider (Anthropic US o DeepSeek CN) sin esta capa. Ver ADR-0020.

- **Capa 1 (text/DOM)**: regex-based redact aplicado a todos los inputs textuales de los 4 agentes (Mapper, Validator, Judge, Remapper). Reemplaza patrones: nombres titulares, números de cuenta (16 dígitos / IBAN), balances numéricos con $ o B/., teléfonos, emails, cédulas PA, direcciones. Implementación: `adapters/llm/pii_filter.py`.
- **Capa 2 (screenshot region fill)**: para Mapper y Remapper (vision), regiones del screenshot que contengan PII se rellenan con color sólido antes del envío. Detección heurística por bounding boxes de OCR. Implementación: `adapters/llm/screenshot_redact.py`.

**AC**: Canary CI tests con `PII_CANARY_NAME`, `PII_CANARY_ACCT`, `PII_CANARY_BAL` inyectados; grep en payloads del model input devuelve 0 hits para los 3. Tests corren en CI bloqueando merge. Operator-overridable via config (opt-in para enviar PII si el operador acepta riesgo legal).

#### REQ-019: browser-use sensitive_data fuzz test (PROMOVIDO A P0)

Validar empíricamente que LLM nunca recibe plaintext. **Issue #3.** **Justificación promoción**: Goal 5 (seguridad de credenciales, zero-tolerance) depende directamente de este test; sin él, no hay garantía empírica del invariante.

**AC**: Test instrumenta LiteLLM, ejecuta Mapper/Remapper sobre fixture con `SECRET_CANARY_VALUE` como password, grep recursivo en payloads del model input devuelve 0 hits. Corre en CI bloqueando merge.

### Should Have (P1)

#### REQ-013: Langfuse self-hosted opcional

Docker-compose service con profile `langfuse`. Off por default (no rompe principio self-host puro). Trazas de Mapper, Validator, Judge, Remapper.

#### REQ-014: OTel para resto

Métricas API + Temporal + Sandbox. Trace correlation con `trace_id` propagado end-to-end.

#### REQ-015: Testing infrastructure

Pyramid: unit (PydanticAI test models, parser DSL contra fixtures), integration (Temporal time-skipping, HAR replay), E2E (smoke contra banco real opt-in via env var).

#### REQ-016: Filter middleware redact + canary CI test

Garantía de que credenciales nunca entran a Langfuse, OTel, HAR, application logs. Canary `SECRET_CANARY_VALUE` + grep en CI. **Issue #1.**

#### REQ-017: Community maps trust model

Linter 12 reglas, cosign signing keyless GitHub OIDC para maps oficiales. Pipeline CI valida community PRs. **Issue #4.**

### Could Have (P2)

> Nota: REQ-018 y REQ-019 fueron promovidos a P0 (ver sección Must Have). Esta sección queda con REQ-020 únicamente.

#### REQ-020: Webhook clock skew /time endpoint

Diagnosis para clientes con NTP roto. **Issue #5.**

---

## Non-Functional Requirements

### Performance

- API `POST /scrape`: response < 1s (devuelve job_id, work async).
- Scrape job end-to-end (banco con 3 cuentas, range 30 días):
  - **`interactive_p99` (excluye tiempo de OTP wait)**: p50 < 90s, p95 < 180s, p99 < 300s. SLO primario.
  - **`wallclock_p99` (incluye OTP wait)**: hard cap 15 min (240s OTP + 60s buffer + scrape budget). SLO secundario, reportado separadamente.
  - **Nota**: p50/p95/p99 reportados como `interactive_*` excluyen explícitamente el tiempo de espera del usuario para confirmar OTP. La métrica `wallclock_*` incluye ese tiempo y tiene cap distinto. Sin esta separación, `p99 < 300s` sería incoherente con OTP cap 240s.
- OTP wait hard cap: 4 min (banco expira sesión a 5).
- Mapper run (one-time): max 5 min, max $0.50 cost.

### Security

- Credenciales bancarias cifradas at-rest con Argon2id + AES-GCM (sqlcipher).
- Master passphrase nunca persiste; solo en memoria.
- **Master passphrase y derived keys protegidos con `mlock(2)`** (no swappeable). Swap deshabilitado en host. Core dumps suppressed (`ulimit -c 0`). Boot-time check enforced (boot falla si swap activo o RLIMIT_CORE != 0). Ver ADR-0008-amendment + `docs/05-operations/deployment.md` M-1..M-7 checklist.
- LLMs nunca reciben plaintext de credenciales (sensitive_data injection en browser-use).
- LLMs nunca reciben PII del usuario en screenshots/DOM (PII redact middleware, ADR-0020, Ley 81 PA Art. 13).
- Network policy estricta por banco en sandbox (allowlist).
- Webhooks firmados HMAC-SHA256 + anti-replay timestamp.
- Threat model STRIDE completo (17 amenazas categorizadas + 9 nuevas post-review = 26) en `docs/04-security/threat-model.md`.

### Reliability

- Durable execution via Temporal: workflows sobreviven restart de worker/server.
- Retry policies con exponential backoff por activity.
- Circuit breakers: 2 fail logins → 1h cooldown.
- Rate limiting por banco: max 1 scrape concurrente por (bank, credential).

### Observability

- 100% de jobs trackeados en Temporal UI.
- Langfuse traces para todos los LLM calls (cuando habilitado).
- Playwright HAR + screenshots por job (sanitizados de creds).
- SLI/SLO definidos: success rate, latencia, costo medio, tasa de remap.

### Compliance / Legal

- License AGPL-3.0 — derivative services deben permanecer abiertos.
- No tracking del usuario (sin telemetry phone-home).
- **Ley 81 PA (protección de datos personales) Art. 13**: la transferencia transfronteriza de datos personales del usuario al LLM provider (Anthropic US, DeepSeek CN) requiere consentimiento explícito y mitigación técnica. open-banca implementa el control técnico via REQ-021 (PII filter middleware, ADR-0020). La capa text + screenshot redact garantiza que PII no abandona el sandbox sin sanitización. **Operador self-hosted** es responsable de: (a) configurar el PII filter correctamente para su jurisdicción, (b) obtener consentimiento del usuario final si aplica, (c) cumplir con auditorías locales (Ley 81 PA, GDPR si UE, otros marcos).

---

## Technical Considerations

### Stack confirmado

| Área | Tech |
|------|------|
| Lenguaje | Python 3.12+ |
| Package mgr | uv |
| Lint | Ruff |
| Type check | Pyrefly |
| Web framework | FastAPI |
| Agents | PydanticAI |
| LLM gateway | LiteLLM |
| Browser agent | browser-use (lib MIT) |
| Scraper | Playwright |
| Excel | openpyxl |
| Workflow | Temporal (Python SDK) |
| Storage | SQLite + sqlcipher |
| Monorepo | Nx + @nxlv/python |
| Sandbox | Docker per-job |
| Observability | Langfuse + OTel |

### Modelos LLM

- Mapper, Remapper: Claude Sonnet 4.6 (vision) via LiteLLM → Anthropic.
- Validator, Judge: DeepSeek V3 (texto) via LiteLLM → DeepSeek.
- Scraper: sin LLM.

### Arquitectura

Hexagonal/Clean. 11 ports nombrados (BankMapPort, BrowserDriverPort, LLMPort, JobStorePort, SecretStorePort, EventBusPort, ExcelParserPort, SandboxPort, ObservabilityPort, ClockPort, WorkflowEnginePort). Adaptadores: api, orchestrator, browser, llm, storage, parsing, sandbox. Detalle en `docs/01-architecture/hexagonal.md`.

### Environment variables clave

| Env var | Default | Descripción |
|---------|---------|-------------|
| `OPEN_BANCA_MASTER_PASSPHRASE` | (none, prompt al boot) | Passphrase maestro para sqlcipher KDF. mlocked en memoria. |
| `BANCA_OTP_SIDECAR_TTL_S` | `300` | TTL en segundos del proceso BrowserSidecar (ADR-0019). Cubre OTP wait 240s + buffer. |
| `OPEN_BANCA_LIVE_SMOKE` | `0` | Si `1`, habilita smoke test contra banco real (requiere creds operador). |
| `TEMPORAL_HOST` | `localhost:7233` | Endpoint Temporal server. |
| `TASK_QUEUE` | `open-banca-default` | Task queue del worker. |

### Decisiones registradas

15 ADRs originales + 5 nuevos post P0 review (0007-amendment, 0008-amendment, 0013-amendment, 0019, 0020) en `docs/adr/`. Source of truth para todos los trade-offs.

---

## Implementation Roadmap

### Phase 1: Foundation (semanas 1–3)

- Monorepo Nx setup + Python uv workspaces.
- Hexagonal scaffolding (domain + application layers).
- FastAPI skeleton con 10 endpoints (stubs).
- Temporal local setup (docker-compose dev).
- SQLite + sqlcipher storage adapter.
- Secret vault con Argon2id + AES-GCM.
- CI: ruff, pyrefly, pytest, canary redact test.

### Phase 2: Scraper runner (semanas 3–5)

- Playwright runner (sin LLM) ejecuta `map.json`.
- 9 step types implementados.
- BreakageEvent contract.
- HAR record/replay testing.

### Phase 3: Mapper + Banco General piloto (semanas 5–8)

- Mapper agent sobre browser-use + Claude vision.
- Primer mapping run real Banco General → produce `map.json`.
- Excel parser DSL engine.
- `parser.json` para Banco General savings + checking + TC.
- Schema canónico unificado.

### Phase 4: Self-healing (semanas 8–10)

- Validator + Judge agents (PydanticAI + DeepSeek).
- Remapper agent.
- Confidence/risk routing (auto vs HITL).
- Proposal endpoints + TTL + git apply.

### Phase 5: Operations + security (semanas 10–12)

- Sandbox Docker per-job + network policies.
- docker-socket-proxy hardening.
- Webhooks HMAC + DLQ.
- Cost guardrails enforcement.
- Observability: Langfuse + OTel.
- docker-compose layout + first-run UX.
- 5 GitHub issues (security risks) cerrados.

### Phase 6: Hardening + release (semanas 12–14)

- E2E smoke con creds reales (3 betatesters).
- Stress test 100 scrapes.
- Documentación completa.
- v1.0.0 tag + release notes.

---

## Out of Scope (v1)

- **Préstamos hipotecarios / personales** (v2).
- **Inversiones, fideicomisos** (v2).
- **Captcha resolving** — bancos PA no lo usan por ahora; cuando aparezca, vision LLM o servicio externo (decisión postergada).
- **UI / frontend** — solo API.
- **Multi-tenant SaaS** — contrato es self-hosted single-org. Multi-tenant lo construye quien quiera encima.
- **Stealth / anti-bot evasion sofisticado** — usamos real browser via CDP; si banco bloquea, escalamos.
- **Otros bancos PA** además de Banco General — BAC, Banistmo, Global Bank son v1.x (post-piloto exitoso).
- **Categorización automática de transacciones** (responsabilidad del cliente).
- **Notificaciones push/email/SMS al usuario final** — solo webhooks.

---

## Open Questions & Risks

### Open Questions (validar al hacer primer mapping real Banco General)

Ver `docs/06-banks/banco-general.md` sección "Open Questions". Resumen:

1. ¿Excel de tarjeta de crédito viene en formato distinto al de savings? (asumido sí).
2. ¿Hay rate limit del banco? (asumido no agresivo).
3. ¿El portal soporta múltiples sesiones concurrentes para misma credencial?
4. ¿Qué pasa si Clave Móvil no llega (push perdido)? ¿Hay retry de OTP?
5. ¿El historial real es de 6 meses o más?
6. ¿Hay diferencias entre el portal personal y el empresarial?

### Risks (gate-keepers, **issues abiertos en GitHub**)

1. **Filter middleware redact + canary CI** ([Issue #1](https://github.com/miketela/open-banca/issues/1)).
2. **docker-socket-proxy hardening** ([Issue #2](https://github.com/miketela/open-banca/issues/2)).
3. **browser-use sensitive_data fuzz** ([Issue #3](https://github.com/miketela/open-banca/issues/3)).
4. **Cosign keyless decision** ([Issue #4](https://github.com/miketela/open-banca/issues/4)).
5. **Webhook clock skew /time** ([Issue #5](https://github.com/miketela/open-banca/issues/5)).

Otros riesgos:

- **Banco bloquea cuenta del usuario por intentos fallidos**. Mitigación: circuit breaker 2 fails → 1h cooldown, dry-run de Remapper antes de apply real.
- **LLM prompt injection desde el banco** (si banco renderiza contenido user-controlled, ej: nombre de beneficiario en transferencia). Mitigación: structured output schemas estrictos, no follow-instructions del DOM.
- **Map malicioso community** que pasa el linter. Mitigación: network whitelist sandbox + monitor egresos.
- **Cosign + Sigstore downtime**: firmas existentes siguen verificables; nuevas firmas blocked. Aceptado.

---

## Validation Checkpoints

### Checkpoint 1 (post-Phase 1): Foundation funciona

- `docker-compose up` arranca todo en host fresh.
- `POST /scrape` devuelve job_id pero workflow es no-op (solo emit `job.completed`).
- Canary test pasa.
- CI green.

### Checkpoint 2 (post-Phase 2): Scraper runner funciona contra HAR

- Replay de HAR fixture produce extracción esperada.
- BreakageEvent emitido cuando selector se rompe en HAR modificado.
- 0 LLM calls en hot path verificado.

### Checkpoint 3 (post-Phase 3): Banco General real

- Primer scrape exitoso contra Banco General real (con creds del operador).
- Excel descargados + parseados.
- Schema canónico válido.

### Checkpoint 4 (post-Phase 4): Self-healing demo

- Mod del map (cambiar selector) → scraper rompe → Judge decide remap → Remapper repara → retry exitoso.
- HITL path testado: confidence < 0.85 → webhook → approve → retry.

### Checkpoint 5 (post-Phase 5): Operations + security

- 5 GitHub issues cerrados.
- Threat model controles implementados.
- Stress test 100 scrapes con success rate ≥ 95%.

### Checkpoint 6 (release): v1.0.0

- 3 betatesters externos completan setup en ≤ 30 min.
- Goals 1–5 medidos y on-track.
- Release notes + docs públicas.

---

## Appendix: Task Breakdown Hints

### Suggested task count

~40–50 tasks principales. ~150–200 subtasks tras `expand --all`.

### Phase distribution

- Phase 1: 8 tasks (incl. T24 docker-socket-proxy promovido, T33 docker-compose stack, T34 CLI)
- Phase 2: 6 tasks (incl. T31 wire endpoints)
- Phase 3: 9 tasks (incl. T32 incremental cursor)
- Phase 4: 7 tasks
- Phase 5: 8 tasks
- Phase 6: 5 tasks (incl. T35 USER-TEST checkpoint 6)
- USER-TEST checkpoints: 6 (tasks 8, 13, 18, 22, 29, 35 — al final de cada phase).

### Source of truth para taskmaster

- `docs/00-overview.md` — visión + stack.
- `docs/DECISIONS.md` — todas las decisiones consolidadas.
- `docs/adr/*.md` — 15 ADRs Nygard.
- `docs/01-architecture/*.md` — 4 docs (macro, hexagonal, multi-agent, data-flow).
- `docs/02-components/*.md` — 10 component specs.
- `docs/03-flows/*.md` — 6 flujos end-to-end.
- `docs/04-security/*.md` — 4 security docs.
- `docs/05-operations/*.md` — 4 ops docs.
- `docs/06-banks/banco-general.md` — banco piloto.

### Convenciones para task breakdown

- Tasks atómicos: 4–8h cada uno.
- Branch naming: `task-{id}-{slug}`.
- Cada task debe tener: descripción, AC (testable), dependencias, estimado.
- USER-TEST tasks cada ~5 tasks (validación humana antes de avanzar).
- Tests obligatorios en cada task de implementación (pyramid).
- Git commit per subtask, merge a task branch al cerrar todos.
